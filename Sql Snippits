SELECT age, COUNT(age) ass occurences
FROM my_user_output_bucket
BROUP BY age
ORDER BY occurences DESC
LIMIT 5;

SELECT gender (COUNT(gender) * 100.0 / (SELECT COUNT(*) FROM <AWS_GLUE_TABLE_NAME>)) AS PERCENT
FROM <AWS_GLUE_TABLE_NAME>
GROUP BY gender;

